import { NamePositionPipe } from './name-position.pipe';

describe('NamePositionPipe', () => {
  it('create an instance', () => {
    const pipe = new NamePositionPipe();
    expect(pipe).toBeTruthy();
  });
});
